version: '3.8'

services:
  app_blue:
    image: poirot92/nodjs-app:latest

  app_green:
    image: poirot92/nodjs-app:latest

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf